# Data directory

Raw datasets are intentionally excluded from the repository. Run:

```bash
python scripts/download_datasets.py --output datasets --verify
```

The exact source URLs and expected SHA-256 hashes are stored in `results/manifests/dataset_raw_manifest.csv`.
