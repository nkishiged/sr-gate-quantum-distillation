# Manuscript support files

This directory contains the curated bibliography and three validated central figures used by the Results section.

The earlier draft `fig_datasets_overview.pdf` is intentionally excluded because its AAPL panel was inconsistent with the raw `Close` target while the caption claimed raw scale. Regenerate and verify that figure before adding it to the manuscript or repository.
